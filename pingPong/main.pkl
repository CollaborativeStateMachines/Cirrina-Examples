amends "https://raw.githubusercontent.com/CollaborativeStateMachines/Cirrina/refs/heads/develop/src/main/resources/pkl/csm/csml.pkl"

collaborativeStateMachine {
  stateMachines {
    ["ping"] {
      transient { ["numPings"] = "50000" }
      states {
        ["ping"] = new Initial {
          entry {
            new Emit { event { topic = "ping" } }
            new Assign { expression = "--numPings" }
          }
          on {
            ["pong"] { 
              to = "ping"; provided = "numPings != 0"; or = "done"
            }
          }
        }
        ["done"] = new Terminal {
          entry { new Emit { event { topic = "done" } } }
        }
      }
    }
    ["pong"] = new StateMachine {
      states {
        ["pong"] = new Initial {
          on {
            ["ping"] { yields { new Emit { event { topic = "pong" } } } }
            ["done"] { to = "done" }
          }
        }
        ["done"] = new Terminal {}
      }
    }
  }
}
instances { 
  ["ping"] { stateMachineName = "ping" }
  ["pong"] { stateMachineName = "pong" }
}