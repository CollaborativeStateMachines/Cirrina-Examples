amends "https://raw.githubusercontent.com/UIBK-DPS-DC/Cirrina-Specifications/main/pkl/JobDescription.pkl"

import "https://raw.githubusercontent.com/UIBK-DPS-DC/Cirrina-Specifications/main/pkl/HttpServiceImplementationDescription.pkl"
import "https://raw.githubusercontent.com/UIBK-DPS-DC/Cirrina-UseCases/main/smart-factory/csml/smart-factory.pkl" as smartFactoryCSM

local serviceHost = read?("env:SIMULATION_SERVICE_HOST") ?? "localhost"
local servicePort = read?("env:SIMULATION_SERVICE_PORT")?.toInt() ?? 8000

collaborativeStateMachine = smartFactoryCSM
stateMachineName = "jobControlSystem" // Change: Replace with state machine name
localData = new Mapping {
  ["totalProducts"] = "100"
  ["partsPerProduct"] = "10"
} // Change: jobControlSystem and roboticArmSystem expect localData (totalProducts, partsPerProduct)
bindEventInstanceIds = new Listing {}
runtimeName = "runtime"

serviceImplementations {
  new HttpServiceImplementationDescription {
    scheme = "http"
    host = serviceHost
    port = servicePort
    endPoint = "/moveBelt"
    method = "POST"
    name = "moveBelt"
    cost = 1.0
    `local` = true
  }
  new HttpServiceImplementationDescription {
    scheme = "http"
    host = serviceHost
    port = servicePort
    endPoint = "/stopBelt"
    method = "POST"
    name = "stopBelt"
    cost = 1.0
    `local` = true
  }
  new HttpServiceImplementationDescription {
    scheme = "http"
    host = serviceHost
    port = servicePort
    endPoint = "/beamDetectionStart"
    method = "POST"
    name = "beamDetectionStart"
    cost = 1.0
    `local` = true
  }
  new HttpServiceImplementationDescription {
    scheme = "http"
    host = serviceHost
    port = servicePort
    endPoint = "/beamDetectionEnd"
    method = "POST"
    name = "beamDetectionEnd"
    cost = 1.0
    `local` = true
  }
  new HttpServiceImplementationDescription {
    scheme = "http"
    host = serviceHost
    port = servicePort
    endPoint = "/takePhoto"
    method = "POST"
    name = "takePhoto"
    cost = 1.0
    `local` = true
  }
  new HttpServiceImplementationDescription {
    scheme = "http"
    host = serviceHost
    port = servicePort
    endPoint = "/scanPhoto"
    method = "POST"
    name = "scanPhoto"
    cost = 1.0
    `local` = true
  }
  new HttpServiceImplementationDescription {
    scheme = "http"
    host = serviceHost
    port = servicePort
    endPoint = "/pickUp"
    method = "POST"
    name = "pickUp"
    cost = 1.0
    `local` = true
  }
  new HttpServiceImplementationDescription {
    scheme = "http"
    host = serviceHost
    port = servicePort
    endPoint = "/assemble"
    method = "POST"
    name = "assemble"
    cost = 1.0
    `local` = true
  }
  new HttpServiceImplementationDescription {
    scheme = "http"
    host = serviceHost
    port = servicePort
    endPoint = "/returnToStart"
    method = "POST"
    name = "returnToStart"
    cost = 1.0
    `local` = true
  }
  new HttpServiceImplementationDescription {
    scheme = "http"
    host = serviceHost
    port = servicePort
    endPoint = "/sendMail"
    method = "POST"
    name = "sendMail"
    cost = 1.0
    `local` = true
  }
  new HttpServiceImplementationDescription {
    scheme = "http"
    host = serviceHost
    port = servicePort
    endPoint = "/sendSms"
    method = "POST"
    name = "sendSms"
    cost = 1.0
    `local` = true
  }
  new HttpServiceImplementationDescription {
    scheme = "http"
    host = serviceHost
    port = servicePort
    endPoint = "/sendStatistics"
    method = "POST"
    name = "sendStatistics"
    cost = 1.0
    `local` = true
  }
}