import "@csm/Csml.pkl"

/// Action that invokes a service that turns the light on.
class LightOnAction extends Csml.InvokeAction {
  serviceType = "lightOn"
}

/// Action that invokes a service that turns the light off.
class LightOffAction extends Csml.InvokeAction {
  serviceType = "lightOff"
}