import "@csm/Csml.pkl"

/// Action that invokes a service that turns the bell on.
class BellOnAction extends Csml.InvokeAction {
  serviceType = "bellOn"
}

/// Action that invokes a service that turns the bell off.
class BellOffAction extends Csml.InvokeAction {
  serviceType = "bellOff"
}